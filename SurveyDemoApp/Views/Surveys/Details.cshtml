@model SurveyDemoApp.ViewModels.QuestionSelectViewModel

@{
    ViewData["Title"] = "Details";
}

<h2>Details</h2>

<div>
    <h4>Survey</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.SurveyTitle)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.SurveyTitle)
        </dd>
        @for (int i = 0; i < Model.QuestionSelections.Count(); i++)
        {
            <dt>
                @Html.DisplayNameFor(model => Model.QuestionSelections[i].Text)
            </dt>
            <dd>
                @Html.DisplayFor(model => Model.QuestionSelections[i].Text)
            </dd>
        }
    </dl>
</div>
<div>
    @*<a asp-action="Edit" asp-route-id="@Model.Survey.Id">Edit</a> |*@
    <a asp-action="Index">Back to List</a>
</div>
